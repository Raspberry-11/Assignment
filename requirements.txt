# DA331 Assignment 2 - DROW 2D range dataset
# Python 3.10-3.12. Requires a JDK (17 or 21) on PATH for PySpark.

pyspark>=3.5,<5.0
pyarrow>=14.0

# PySpark 4.2 does not yet fully support pandas 3.x and emits a compatibility
# warning on every toPandas() call. Held below 3.0 deliberately.
pandas>=2.0,<3.0

numpy>=1.26
scikit-learn>=1.4
matplotlib>=3.8
scipy>=1.11
jupyter>=1.0
